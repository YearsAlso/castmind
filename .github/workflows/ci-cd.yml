name: 🐳 CastMind CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main, develop ]
  schedule:
    # 每天凌晨2点运行测试
    - cron: '0 2 * * *'
  workflow_dispatch:  # 手动触发

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # ============================================
  # 代码质量检查
  # ============================================
  code-quality:
    name: 🔍 代码质量检查
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4
    
    - name: 🐍 设置 Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'
        cache: 'pip'
    
    - name: 📦 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements-dev.txt
    
    - name: 🧹 代码格式化检查 (Black)
      run: |
        black --check --diff .
    
    - name: 📏 代码风格检查 (Flake8)
      run: |
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics
    
    - name: 🔍 类型检查 (MyPy)
      run: |
        mypy . --ignore-missing-imports
    
    - name: 🐕 安全检查 (Bandit)
      run: |
        bandit -r . -f json -o bandit-report.json || true
    
    - name: 🚨 依赖安全检查 (Safety)
      run: |
        safety check --full-report
    
    - name: 🎯 代码复杂度检查
      run: |
        radon cc . -a -s
    
    - name: 📊 代码重复度检查
      run: |
        flake8 . --select=RST

  # ============================================
  # 单元测试
  # ============================================
  unit-tests:
    name: 🧪 单元测试
    runs-on: ubuntu-latest
    needs: code-quality
    
    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4
    
    - name: 🐍 设置 Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'
        cache: 'pip'
    
    - name: 📦 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements-dev.txt
    
    - name: 🧪 运行单元测试
      run: |
        pytest tests/ -v --cov=. --cov-report=xml --cov-report=html
    
    - name: 📊 上传测试覆盖率
      uses: codecov/codecov-action@v4
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
    
    - name: 📁 上传测试报告
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: test-reports
        path: |
          htmlcov/
          test-results.xml
        retention-days: 30

  # ============================================
  # 集成测试
  # ============================================
  integration-tests:
    name: 🔗 集成测试
    runs-on: ubuntu-latest
    needs: unit-tests
    
    services:
      redis:
        image: redis:7-alpine
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379
    
    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4
    
    - name: 🐍 设置 Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.9'
        cache: 'pip'
    
    - name: 📦 安装依赖
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements-dev.txt
    
    - name: 🔧 设置测试环境变量
      run: |
        echo "REDIS_URL=redis://localhost:6379/0" >> $GITHUB_ENV
        echo "DATABASE_URL=sqlite:///test.db" >> $GITHUB_ENV
        echo "APP_ENV=testing" >> $GITHUB_ENV
    
    - name: 🧪 运行集成测试
      run: |
        pytest tests/integration/ -v --tb=short
    
    - name: 📁 上传集成测试报告
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: integration-test-reports
        path: integration-test-results.xml
        retention-days: 30

  # ============================================
  # Docker 构建
  # ============================================
  docker-build:
    name: 🐳 Docker 构建
    runs-on: ubuntu-latest
    needs: [unit-tests, integration-tests]
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    
    permissions:
      contents: read
      packages: write
    
    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4
    
    - name: 🐳 设置 Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: 🔐 登录到容器注册表
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    
    - name: 🏷️ 提取元数据
      id: meta
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
        tags: |
          type=ref,event=branch
          type=ref,event=pr
          type=semver,pattern={{version}}
          type=semver,pattern={{major}}.{{minor}}
          type=sha,prefix={{branch}}-
    
    - name: 🐳 构建并推送 Docker 镜像
      uses: docker/build-push-action@v5
      with:
        context: .
        push: ${{ github.event_name != 'pull_request' }}
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
        platforms: linux/amd64,linux/arm64
        target: production
    
    - name: 🐳 构建开发镜像
      uses: docker/build-push-action@v5
      with:
        context: .
        push: ${{ github.event_name != 'pull_request' }}
        tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest-dev
        cache-from: type=gha
        cache-to: type=gha,mode=max
        platforms: linux/amd64,linux/arm64
        target: development
    
    - name: 📊 镜像扫描 (Trivy)
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
        format: 'sarif'
        output: 'trivy-results.sarif'
    
    - name: 📁 上传安全扫描报告
      uses: actions/upload-artifact@v4
      with:
        name: trivy-scan-report
        path: trivy-results.sarif
        retention-days: 30
    
    - name: 🚨 上传安全扫描结果到 GitHub
      uses: github/codeql-action/upload-sarif@v3
      if: always()
      with:
        sarif_file: 'trivy-results.sarif'

  # ============================================
  # 部署到测试环境
  # ============================================
  deploy-staging:
    name: 🚀 部署到测试环境
    runs-on: ubuntu-latest
    needs: docker-build
    if: github.ref == 'refs/heads/develop' && github.event_name == 'push'
    environment:
      name: staging
      url: https://staging.castmind.example.com
    
    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4
    
    - name: 🔐 配置 SSH
      uses: webfactory/ssh-agent@v0.9.0
      with:
        ssh-private-key: ${{ secrets.STAGING_SSH_KEY }}
    
    - name: 🚀 部署到测试服务器
      env:
        DEPLOY_HOST: ${{ secrets.STAGING_HOST }}
        DEPLOY_USER: ${{ secrets.STAGING_USER }}
      run: |
        ssh -o StrictHostKeyChecking=no ${DEPLOY_USER}@${DEPLOY_HOST} << 'EOF'
          cd /opt/castmind-staging
          git pull origin develop
          docker-compose pull
          docker-compose up -d --build
          docker system prune -f
        EOF
    
    - name: 🏥 健康检查
      run: |
        sleep 30  # 等待服务启动
        curl -f https://staging.castmind.example.com/api/v1/health || exit 1
    
    - name: 📧 发送部署通知
      if: success()
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: smtp.gmail.com
        server_port: 465
        username: ${{ secrets.MAIL_USERNAME }}
        password: ${{ secrets.MAIL_PASSWORD }}
        subject: "✅ CastMind 测试环境部署成功"
        to: ${{ secrets.NOTIFICATION_EMAIL }}
        from: CastMind CI/CD <ci@castmind.example.com>
        body: |
          🎉 CastMind 测试环境部署成功！
          
          分支: ${{ github.ref }}
          提交: ${{ github.sha }}
          时间: $(date)
          
          查看详情: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          
          服务地址: https://staging.castmind.example.com

  # ============================================
  # 部署到生产环境
  # ============================================
  deploy-production:
    name: 🚀 部署到生产环境
    runs-on: ubuntu-latest
    needs: docker-build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    environment:
      name: production
      url: https://castmind.example.com
    
    steps:
    - name: 📥 检出代码
      uses: actions/checkout@v4
    
    - name: 🔐 配置 SSH
      uses: webfactory/ssh-agent@v0.9.0
      with:
        ssh-private-key: ${{ secrets.PRODUCTION_SSH_KEY }}
    
    - name: 🏷️ 创建 Git 标签
      if: startsWith(github.ref, 'refs/tags/')
      run: |
        TAG_NAME=${GITHUB_REF#refs/tags/}
        git tag -a "deploy-$TAG_NAME" -m "Deployed to production $TAG_NAME"
        git push origin "deploy-$TAG_NAME"
    
    - name: 🚀 部署到生产服务器
      env:
        DEPLOY_HOST: ${{ secrets.PRODUCTION_HOST }}
        DEPLOY_USER: ${{ secrets.PRODUCTION_USER }}
      run: |
        ssh -o StrictHostKeyChecking=no ${DEPLOY_USER}@${DEPLOY_HOST} << 'EOF'
          cd /opt/castmind-production
          git pull origin main
          docker-compose pull
          docker-compose up -d --build
          docker system prune -f
          # 备份数据库
          docker exec castmind-postgres pg_dump -U castmind castmind > /backups/castmind_$(date +%Y%m%d_%H%M%S).sql
          # 保留最近7天的备份
          find /backups -name "castmind_*.sql" -mtime +7 -delete
        EOF
    
    - name: 🏥 健康检查
      run: |
        sleep 60  # 等待服务启动
        curl -f https://castmind.example.com/api/v1/health || exit 1
    
    - name: 📊 性能测试
      run: |
        # 简单的性能测试
        ab -n 100 -c 10 https://castmind.example.com/api/v1/health || true
    
    - name: 📧 发送生产部署通知
      if: success()
      uses: dawidd6/action-send-mail@v3
      with:
        server_address: smtp.gmail.com
        server_port: 465
        username: ${{ secrets.MAIL_USERNAME }}
        password: ${{ secrets.MAIL_PASSWORD }}
        subject: "🚀 CastMind 生产环境部署成功"
        to: ${{ secrets.NOTIFICATION_EMAIL }}
        from: CastMind CI/CD <ci@castmind.example.com>
        body: |
          🎉 CastMind 生产环境部署成功！
          
          版本: ${{ github.ref }}
          提交: ${{ github.sha }}
          时间: $(date)
          
          查看详情: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          
          服务地址: https://castmind.example.com
          
          请进行最终验证。

  # ============================================
  # 清理工作流
  # ============================================
  cleanup:
    name: 🧹 清理工作流
    runs-on: ubuntu-latest
    if: always()
    
    steps:
    - name: 🗑️ 清理 Docker 缓存
      run: |
        docker system prune -f --filter "until=24h"
    
    - name: 📊 工作流总结
      if: always()
      run: |
        echo "🎯 工作流执行完成"
        echo "状态: ${{ job.status }}"
        echo "运行时间: ${{ job.steps.*.run_duration }}"
        echo "详细信息: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"